{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the selector desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "fs:default", "sql:default"]
}
